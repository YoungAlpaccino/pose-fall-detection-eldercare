# Eldercare fall detection — Python dependencies (loosely pinned)

# Training & export
torch>=2.2,<3.0
onnx>=1.15,<2.0

# Inference runtime (edge + backend)
onnxruntime>=1.17,<2.0

# CV / numerics
numpy>=1.26,<3.0
opencv-python>=4.9,<5.0

# Pose estimation
mediapipe>=0.10,<0.11

# Geometry / metrics / signal smoothing
scipy>=1.12,<2.0
scikit-learn>=1.4,<2.0

# Backend
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
sqlmodel>=0.0.16,<0.1
pydantic>=2.6,<3.0
python-jose[cryptography]>=3.3,<4.0
websockets>=12.0,<13.0

# Testing
pytest>=8.0,<9.0
